/**
 * TypeScript types for apibuilder generator
 * Based on API Builder specification at io.apibuilder.generator.v0
 *
 * Generated by API Builder - do not edit manually
 */

import type { Service } from './io-apibuilder-spec-v0.ts';

// ============================================================================
// Enums
// ============================================================================

export enum FileFlag {
  Scaffolding = 'scaffolding',
}

// ============================================================================
// Models
// ============================================================================

/**
 * An attribute represents a key/value pair that is optionally used to provide additional instructions / data to the code generator. An example could be an attribute to specify the root import path for a go client..
 */
export interface Attribute {
  name: string;
  value: string;
}

export interface Error {
  /** Machine readable code for this specific error message */
  code: string;
  /** Description of the error */
  message: string;
}

/**
 * Represents a source file
 */
export interface File {
  /** The recommended name for the file. */
  name: string;
  /** The recommended directory path for the file where appropriate. */
  dir?: string;
  /** The actual source code. */
  contents: string;
  flags?: FileFlag[];
}

/**
 * The generator metadata.
 */
export interface Generator {
  key: string;
  name: string;
  /** A comma separate list of the programming language(s) that this generator produces */
  language?: string;
  description?: string;
  /** The list of attributes that this code generator can use. You can find the full list of available attributes and their descriptions at http://apibuilder.io/doc/attributes */
  attributes: string[];
}

export interface Healthcheck {
  status: string;
}

/**
 * The result of invoking a generator.
 */
export interface Invocation {
  /** The actual source code. */
  source: string;
  /** A collection of source files */
  files: File[];
}

/**
 * The invocation form is the payload send to the code generators when requesting generation of client code.
 */
export interface InvocationForm {
  service: Service;
  attributes: Attribute[];
  user_agent?: string;
  imported_services?: Service[];
}

// ============================================================================
// API Client
// ============================================================================

import { VoidResponse } from './generated-error-void-response.ts';
import { ErrorsResponse } from './generated-error-errors-response.ts';
import { ApiException } from "./generated-util.ts";

export interface GetGeneratorsOptions {
  limit: number;
  offset: number;
  key?: string;
  headers?: Record<string, string>;
}

export interface GetGeneratorByKeyOptions {
  headers?: Record<string, string>;
}

export interface GetHealthcheckOptions {
  headers?: Record<string, string>;
}

export interface CreateInvocationByKeyOptions {
  key: string;
  body: InvocationForm;
  headers?: Record<string, string>;
}

export class ApiClient {
  private baseUrl: string;

  constructor(baseUrl: string) {
    this.baseUrl = baseUrl;
  }

  async getGenerators(params: GetGeneratorsOptions): Promise<Generator[]> {
    const queryParts: string[] = [];
    if (params.key !== undefined && params.key !== null) {
      queryParts.push(`key=${encodeURIComponent(params.key)}`);
    }
    queryParts.push(`limit=${encodeURIComponent(String(params.limit))}`);
    queryParts.push(`offset=${encodeURIComponent(String(params.offset))}`);
    const queryString = queryParts.length > 0 ? '?' + queryParts.join('&') : '';
    const url = `${this.baseUrl}/generators${queryString}`;

      const response = await fetch(url, {
      method: 'GET',
      headers: {
        'Content-Type': 'application/json',
        ...(params.headers || {}),
      },
    });

    if (response.status === 200) {
      const data = await response.json();
      return data;
    }

    throw new ApiException(response, `Request failed with status ${response.status}`);

  }

  async getGeneratorByKey(key: string, options?: GetGeneratorByKeyOptions): Promise<Generator> {
    const url = `${this.baseUrl}/generators/${key}`;

      const response = await fetch(url, {
      method: 'GET',
      headers: {
        'Content-Type': 'application/json',
        ...(options?.headers || {}),
      },
    });

    if (response.status === 200) {
      const data = await response.json();
      return data;
    }

    if (response.status === 404) {
      throw new VoidResponse(response);
    }

    throw new ApiException(response, `Request failed with status ${response.status}`);

  }

  async getHealthcheck(params: GetHealthcheckOptions): Promise<Healthcheck> {
    const url = `${this.baseUrl}/_internal_/healthcheck`;

      const response = await fetch(url, {
      method: 'GET',
      headers: {
        'Content-Type': 'application/json',
        ...(params.headers || {}),
      },
    });

    if (response.status === 200) {
      const data = await response.json();
      return data;
    }

    throw new ApiException(response, `Request failed with status ${response.status}`);

  }

  async createInvocationByKey(params: CreateInvocationByKeyOptions): Promise<Invocation> {
    const url = `${this.baseUrl}/invocations/${params.key}`;

      const response = await fetch(url, {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        ...(params.headers || {}),
      },
      body: JSON.stringify(params.body),
    });

    if (response.status === 200) {
      const data = await response.json();
      return data;
    }

    if (response.status === 409) {
      throw new ErrorsResponse(response);
    }

    throw new ApiException(response, `Request failed with status ${response.status}`);

  }

}